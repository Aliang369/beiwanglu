# 未完成与预留功能清单

> 盘点日期：2026-07-21（第二节状态已按源码复核更新）  
> 范围：知识库开局认知 + 仓库 `docs/` + 源码交叉核对  
> 粒度：功能级（功能名 + 状态 + 一句话说明 + 主要位置）  
> 说明：本清单为盘点结果，**不代表排期承诺**；与过时文档冲突时，以代码与知识库（`功能状态总览` / `产品定位`）为准。

## 状态用语

| 用语 | 含义 |
| --- | --- |
| 部分可用 | 核心可用，缺关键能力或仍有演示区 |
| 原型 | UI/流程演示，未接真实业务数据 |
| 占位 | 仅类型/入口预留，无实现 |
| 基础可用 | 能跑，但离产品级/发布级仍缺配置或能力 |
| 未实现 | 路线图或代码明确预留，当前无业务能力 |
| 文档过时 | 文档描述与当前代码/知识库不一致 |

---

## 一、部分可用 / 原型 / 占位

| 功能 | 状态 | 一句话说明 | 主要位置 |
| --- | --- | --- | --- |
| 设置页 | 部分可用 | 资料/改密/头像/同步开关已接 | `SettingsView.tsx` |
| 帮助页 | 原型 | 分类与文章列表为静态文案，搜索与文章无真实内容/跳转 | `HelpView.tsx` |
| 编辑器信息面板 | 部分可用 | 已有字数/阅读时间/时间戳/标签/置顶/只读；仍按产品侧「部分可用」保留，未做更深属性与统计 | `EditorInfoPanel.tsx` |
| 笔记公开链接 | 未实现（入口预留） | 分享区「公开链接」标「暂未开放」，需公开页与权限服务 | `EditorView.tsx`（分享相关区块） |
| SQLite 数据源 | 可用 | Web sql.js + 失败回退 localStorage；桌面优先原生 SQLite 文件 | `src/shared/data/sqlite/`、`src-tauri`、`localBackend.ts` |
| 移动端数据源 | 部分可用 | 复用统一 SQLite schema/入口；原生驱动未接 | `mobileNotesRepository.ts` |
| Tauri 桌面壳 | 可用 | 窗口/图标/CSP/原生 SQLite command/托盘/菜单/导入导出/快捷键 | `src-tauri/`、`docs/tauri.md` |
| 隐私协议 / 服务条款正文 | 部分可用 | 有弹窗骨架与文案入口，产品化正式条款仍属后续（见路线图 P5） | `AgreementModal.tsx`、`docs/roadmap.md` |

---

## 二、代码 TODO / 未接线 / 预留实现

> 状态复核：2026-07-21（对照当前源码）。「已修正」仅表示注释/文档债已清；「部分可用」表示主路径已落地但能力未完整。

| 项 | 状态 | 一句话说明 | 主要位置 |
| --- | --- | --- | --- |
| 快照存储迁移注释 | 已修正（2026-07-21） | `snapshot.ts` / `snapshotsRepository.ts` / `notesStore` 注释已改为本机仓储 + 同步引擎，不再写登录硬切远端 | `src/shared/types/snapshot.ts`、`snapshotsRepository.ts`、`notesStore.ts` |
| `notesApi` 文件头注释 | 已修正（2026-07-21） | 注释说明本地优先；API 供同步推拉 | `notesApi.ts` |
| 公开分享链接 | 未接线 | UI 虚线卡片 +「暂未开放」，无公开页/权限服务 | `EditorView.tsx` |
| Refresh Token | 已实现（2026-07-21） | access 15m + refresh 30d；`/auth/refresh` 轮换；httpClient 401 自动续期；token 存 localStorage | `authStore.ts`、`tokenStorage.ts`、`httpClient.ts`、`refresh_tokens` |
| 本地 → 云端数据迁移 | 可用 | 启动导入 localStorage→SQLite；登录且同步开启时 LWW 合并上传（保留本地 id） | `migrateLegacyToSqlite.ts`、`syncEngine.ts`、`localStorageSyncEngine.ts` |
| 同步冲突解决 | 可用（LWW） | SQLite 队列 LWW + 失败重试上限；localStorage 回退 LWW；不做字段级/人工选版本 | `src/shared/sync/` |
| 消息实时推送 | 未实现 | 无 WebSocket/推送通道；依赖拉取 | `messagesStore` |
| 自动化测试 | 部分可用 | Vitest 已覆盖 `noteDomain` / `noteSelectors` / `folderDomain`；无组件测 / E2E / 覆盖率 | `package.json`、`src/shared/notes/*.test.ts`、`docs/testing.md` |
| CI | 已实现（前端门禁） | PR + 推送 `main` 自动跑 lint / test / build | `.github/workflows/ci.yml` |
| Tauri 自定义 command | 已实现 | 原生 SQLite open/exec/query/batch + 读文本文件 | `src-tauri/src/main.rs` |
| Tauri bundle 图标 | 已完成 | `bundle.icon` 已配置 icns/ico/png | `tauri.conf.json` |
| Tauri CSP | 已完成 | 已启用兼容前端的 CSP（含 API/字体/YouTube） | `tauri.conf.json` |
| 文件夹任意深度嵌套 | 未实现 | 领域层强制一层 `parentId`（`assertValidParentId`） | `folderDomain.ts`、`docs/roadmap.md` |
| 回收站到期提醒 | 未实现 | 有 30 天清理与末 3 天高亮，无独立到期提醒 | `TrashView`、`docs/feature-status.md` |
| 回收站批量恢复 | 未实现 | 路线图可选后续 | `docs/feature-status.md` |
| Markdown/JSON 导入 | 已实现 | 设置页/菜单导入 .md/.json | `noteImport.ts`、SettingsView、菜单事件 |
| JSON 备份导出 | 已实现 | 设置页导出全部未删除笔记 JSON（与导入兼容，不含回收站） | `noteExportBackup.ts`、SettingsView |
| 桌面原生通知 | 未实现 | 路线图 P4；需 Tauri notification 等 | `docs/roadmap.md` |
| 自动更新 / 签名公证 | 未实现 | 发布级配置未做 | `docs/tauri.md`、`docs/roadmap.md` |
| 全局错误收集与日志 | 未实现 | 产品化清单项 | `docs/roadmap.md` P5 |
| 离线字体（去 CDN） | 未实现 | 产品化清单项 | `docs/roadmap.md` P5 |
| 可访问性专项 | 未实现 | 产品化清单项 | `docs/roadmap.md` P5 |

---

## 三、文档写明「下期 / 未来 / 路线图」的能力


来源以 `docs/roadmap.md`、`docs/feature-status.md`「可选后续」、`产品定位` 为主（功能级摘要）。
状态复核：2026-07-21（对照当前源码 / 知识库，不代表排期承诺）。

| 阶段/来源 | 状态 | 能力摘要 | 完成项 | 未完成项 |
| --- | --- | --- | --- | --- |
| P0 基座 | 已完成 | lint/build、shared 单测、`.trae` lint 策略、CI | Vitest + `npm test`；3 个 domain 单测文件；oxlint 忽略 `.trae/**` 与 `public/sql.js/**`；GitHub Actions 跑 lint/test/build | 组件测 / E2E / 覆盖率不在 P0 范围，见后续测试分层 |
| P1 后端 | 已完成 | 真实 auth/messages/user + Refresh Token | 用户名密码账号；双 Token 自动续期；验证码/邮箱/第三方登录/删号已移出范围 | — |
| P2 云同步 | 已完成 | 稳健性产品化：队列重试、id 保留、消息/快照同步、状态可感知 | 失败重试上限 8；登录/回前台/online/本地写入防抖同步；设置页待同步/失败计数；消息离线已读入队；LWW | 字段级冲突 UI、E2E 加密、实时推送不在范围 |
| P3 桌面 | 已完成（可用桌面级） | 原生 SQLite、导入/导出备份、图标/CSP、托盘/菜单/快捷键 | 图标/CSP；原生 DB command；MD/JSON 导入；JSON 备份导出；托盘与菜单；Cmd/Ctrl+N/K | updater/签名公证属 P5；原生通知属 P4 |
| P4 消息协作 | 未完成 | 真推送、原生通知、权限、评论/分享/提醒 | 消息中心可拉取（Mock/Real）；分类筛选 UI 已有 | 无 WebSocket/推送；无桌面原生通知；评论/分享/多人提醒按产品定位不做团队协作 |
| P5 发布 | 未完成 | 签名公证、自动更新、错误收集、隐私协议、离线字体、a11y | 有协议弹窗骨架（示例文案） | 无签名/公证/自动更新/错误收集；字体仍走 Google Fonts CDN；无 a11y 专项；正式隐私条款未产品化 |
| 可选增强 | 未完成 | 文件夹任意深度、回收站提醒/批量恢复、消息分类筛选 | 消息中心按 category 筛选已可用 | 文件夹仍强制一层；无回收站到期提醒与批量恢复 |

---

## 四、文档过时 / 状态不准

> **2026-07-21 已定点修正**：`README.md`、`docs/feature-status.md`、`docs/roadmap.md`、`docs/api-contract.md`、`docs/data-model.md`、`docs/architecture.md`、`docs/testing.md`，以及 `notesApi.ts` / `snapshot.ts` / `snapshotsRepository.ts` / `notesStore` 本地优先注释。知识库 `功能状态总览` + `INDEX` 已挂接本清单路径。

历史过时点（供对照，勿再按旧文排期）：

| 文档 | 曾过时说法 | 实际现状（摘要） |
| --- | --- | --- |
| `README.md` 功能状态表 | 回收站/文件夹/消息/账号/导出/历史标原型或部分可用 | 主路径已可用；设置部分可用、帮助原型 |
| `docs/feature-status.md` | 远程笔记未接入；删夹进废纸篓；全 Mock | 登录注入远端仓储；删夹→未分类；real 可联调 |
| `docs/roadmap.md` | 待真实后端；apiNotes 未注入 | 后端 7 模块 + 仓储注入已完成 |
| `docs/api-contract.md` | 后端未就绪；notes 不替换本地仓储 | mock/real 双模式；本地优先 + 同步推拉 |
| `docs/data-model.md` | 删夹进废纸篓；快照仅 localStorage | 未分类；本机快照 + 同步推拉 |
| `docs/architecture.md` / `testing.md` | apiNotes 未接入 / 登录硬切 | 本地优先本机仓储 + 同步引擎 |
| 源码注释 | notesApi/snapshot 旧表述 | 已按未登录/登录双路径修正 |

---

## 五、明确不做（产品定位）

来自知识库 / 产品定位「不做的事」，**不是未完成 backlog**，单独列出以免误排期：

| 项 | 说明 |
| --- | --- |
| 团队协作 / 多人编辑 | 个人笔记定位 |
| 实时同步 / 冲突合并 | 本期多端仅 last-write-wins 基础，不做完整 CRDT/合并 |
| 加密存储 | 本地明文；远端 JWT 鉴权隔离 |
| 插件系统 / 第三方集成 | 除 YouTube 嵌入外不做插件生态 |
| AI 辅助写作 | 不在产品定位内 |

---

## 六、盘点结论（给后续排期用）

1. **产品主路径（笔记 CRUD / 文件夹 / 回收站 / 搜索 / 导出 / 快照 / 登录 / 同步 / 桌面可用级）**：代码与文档/知识库均按 **P0–P3 已完成** 对齐；主路径以「可用」为主。  
2. **真实缺口集中在**：帮助页完善、公开链接、组件/E2E 测试、移动端原生 SQLite、P4（推送/原生通知）、P5（签名公证/自动更新/正式协议/离线字体/a11y）。  
3. **入口已画未做**：公开链接。帮助页仍为原型。  
4. **维护建议**：以本文件 + 知识库 `功能状态总览` 为「未完成真相」；改功能后同步 `README` / `feature-status` / `roadmap` / `api-contract` 与知识库，避免再次漂移。  
5. **文档债说明（2026-07-22）**：已全面修订 README 与关键 docs，消除「仍像早期原型」的过时表述；若再发现漂移，优先改本清单第三节状态表。

## 相关文档

- `docs/feature-status.md`
- `docs/roadmap.md`
- `docs/api-contract.md`
- `docs/tauri.md`
- `docs/testing.md`
- `README.md`
- 知识库：`功能状态总览`、`产品定位`、`项目演进时间线`
